const readline = require("readline");

class Parser{
  static input = undefined;
  constructor(){
    input = input || init();
  }
  parse = () => {
    
  }
  init = () => {
    return readline.createInterface({
      input: process.stdin,
      output: null 
    }) 
  }
}
